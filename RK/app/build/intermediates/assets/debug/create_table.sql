CREATE TABLE IF NOT EXISTS story (
id               int,
subtext          text,
maintext         text
);

CREATE TABLE IF NOT EXISTS character (
id               int,
img              int,
subtext          text,
maintext         text
);

